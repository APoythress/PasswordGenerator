using PasswordGenerator.Models;

namespace PasswordGenerator.Controllers
{
    public class GenerateRandomPasswordController
    {

        // Method for number of characters requested
        public void NumberOfCharacters(int numberOfCharacters)
        {
            GenerateRandomPasswordViewModel model = new GenerateRandomPasswordViewModel();
        }
    }
}
